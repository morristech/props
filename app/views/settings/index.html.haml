.row
  .col-xs-12
    %h1 Settings
    %h2 Email subscription

    %p
      Emails will be sent to
      %strong
        = " #{current_user.email}"

    = form_for mail_subscription, url: apply_settings_path, method: :post do |f|
      .form-group
        .checkbox
          = label_tag do
            = f.check_box :active
            Subscription active?
      .form-group
        .radio
          = label_tag do
            = f.radio_button("interval", "daily")
            Daily
        .radio
          = label_tag do
            = f.radio_button("interval", "weekly")
            Weekly
      = f.submit
.row
  .col-xs-12
    %h2 Notifications Channel
    - if current_user.admin?
      = form_for organisation, url: save_slack_channel_settings_path, method: :post do |f|
        .form-group
          = f.text_field :slack_channel
          %p If not set, default slack channel is '#{AppConfig.slack.default_channel}'
        = f.submit "Update Slack Channel"
